package backup

// Backup tests are temporarily disabled during the SQLite removal.
// The backup system needs to be reimplemented for PostgreSQL (pg_dump based).
// TODO: rewrite backup tests once PostgreSQL backup implementation is complete.
